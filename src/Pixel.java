public class Pixel { //vai identificar todos os pontos/pixels da imagem (lembrar de matriz)
    final int x, y; //o final é uma característica que impõe que a informação nunca vai mudar
    Pixel(int x, int y) { this.x = x; this.y = y; }
    /*
    pensando na criação de novos pixels, essa última linha é quem faz isso e guarda em x e y
     */
}

//acho q essa classe pode deixar de ser usada se formos realmente usar o point do java (duvidoso já q já é uma classe pronta)